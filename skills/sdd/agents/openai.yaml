interface:
  display_name: "SDD Through Swingle Delegate"
  short_description: "Execute an implementation plan through subagent-driven-development, using swingle-delegate at each external dispatch point."
  default_prompt: "Execute this implementation plan with the swingle-sdd skill."

policy:
  allow_implicit_invocation: true
