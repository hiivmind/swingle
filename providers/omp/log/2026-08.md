# SDD Dispatch Verification Log — omp, 2026-08

Append-only. Never rewrite prior entries — a later contradiction dates a behavior change.
Format per [verification-protocol.md](../../../core/verification-protocol.md).
Chronological within shard; older and newer shards are siblings in this directory.

---

## 2026-08-02 — new provider: omp (Oh My Pi) 17.2.4 (trigger: add omp as a dispatch target)

Established the omp provider pack. All manifest facts verified live against omp 17.2.4.

**Mechanics (verified):**

| Probe | Assertion | Verdict | Evidence |
| --- | --- | --- | --- |
| version | `omp --version` | **Confirmed** | `omp/17.2.4`, exit 0 (also exit 0 under isolated empty `HOME`) |
| session-source | id in print output | **Confirmed** | `--mode json` line 1 = `{"type":"session",...,"id":"019fc4b0-…"}`; also written as `<ts>_<uuid>.jsonl` in `--session-dir` |
| resume | `omp -p -r <id>` restores context | **Confirmed** | resumed `019fc4b0` by 8-char prefix → model recalled prior-turn `PONG` |
| report-transport | captured-output | **Confirmed** | final answer = last assistant message in terminal `agent_end` (`isTerminal:true`); `--mode text` = clean stdout, spinner on stderr |
| stall-signal | log-age | **Confirmed** | `--mode json` streams `message_update`/`tool_execution_*` deltas continuously — a hang stops emitting |
| sandbox | none + headless tools | **Confirmed** | `--auto-approve` dispatch created `pong.txt` (`write`) and ran `cat` (`bash`) non-interactively, exit 0 |
| readiness | no exit-code auth gate | **Confirmed (omitted)** | `omp models` exits **0** even unauthenticated ("No models available. Set API keys…"); `omp --version` also ungated → no `readiness-argv`; preflight → `available (auth unverified): omp` |

**Models (dispatch-verified through omp):**

| Model | Backend | Probe | Verdict |
| --- | --- | --- | --- |
| `gemini-3.6-flash` | google-antigravity | PONG + resume | **verified** (cheapest/any P1) |
| `claude-sonnet-4-5` | anthropic (Max) | full tool-use: write + bash under `--auto-approve` | **verified** (standard/implement P1) |
| `claude-opus-4-5` | anthropic (Max) | PONG | **verified** (most-capable/implement P1) |
| `gpt-5.6-terra` | openai-codex | PONG | **experimental** — review P1; P13 pending |
| `gpt-5.6-sol` | openai-codex | PONG | **experimental** — review P1; P13 pending |

All three subscription backends (Antigravity / Anthropic Max / ChatGPT-codex) confirmed
routable through omp. Metered `opencode-go` intentionally kept off the default tier routes.

**Follow-ups (documented, not fabricated):**
- Review-lane P13 (severity-qualification) through omp for `gpt-5.6-terra` / `gpt-5.6-sol` —
  they stay `experimental` (eligible, flagged) until it passes.
- P2 fallbacks (`claude-haiku-4-5`, `gpt-5.5`) seeded `experimental`, not yet dispatched.
- An omp **controller/harness** adapter (`controllers/omp.md`, `skills/sdd/harnesses/omp.md`)
  so omp can itself drive swingle — out of scope for this pack (dispatch-target only).

---
