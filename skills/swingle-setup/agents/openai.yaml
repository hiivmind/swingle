interface:
  display_name: "Swingle Setup"
  short_description: "Manage Swingle-owned configuration, preferences, and ledgers with explicit consent for writes."
  default_prompt: "Manage Swingle-owned state with the swingle-setup skill."

policy:
  allow_implicit_invocation: false
