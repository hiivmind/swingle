# grok models (Grok Build / xAI) — verified dispatching 2026-07-24

> The table of record is [models.yaml](models.yaml) (layered overrides: see README
> "Model tables and overrides"). This file carries the documentary layer only —
> verification narrative, watch lists, and corrections.

Effort: `--reasoning-effort` / `--effort` — for `grok-4.5`: `low|medium|high` (default `high`).
Tiering: cheapest → `low`, standard → `medium`, most-capable → `high`.

## Documentary

(none yet)

## Watch list

- New models listed by `grok models` after CLI updates — slot into the table only after a dispatch check.
