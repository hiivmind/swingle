# SDD Dispatch Verification Log — codex, 2026-08

Append-only. Never rewrite prior entries — a later contradiction dates a behavior change.
Format per [verification-protocol.md](../../../core/verification-protocol.md).
Chronological within shard; older and newer shards are siblings in this directory.

---

## 2026-08-02 — readiness-argv added: `codex login status` (trigger: Step-0 Part B — honest readiness; 0.146.0)

Added `readiness-argv: ["codex", "login", "status"]` so Step-0 readiness proves
authentication, not just CLI presence. Previously readiness fell back to `codex
--version`, which succeeds while logged out, so a dead-auth codex was reported `ready:`.

**Auth-gating verified live (0.146.0):**
- Logged in: `codex login status` → "Logged in using ChatGPT", exit 0.
- Isolated logged-out (`CODEX_HOME=$(mktemp -d)`, real creds untouched): "Not logged in", exit 1.

Cheap, non-mutating, no credential in output. Real user auth now yields `ready: codex`
instead of `available (auth unverified): codex`.

---
