interface:
  display_name: "Delegate to External CLI"
  short_description: "Delegate one explicitly requested self-contained job or homogeneous batch through an installed provider CLI using Swingle contracts, live help, configuration, and ledgers."
  default_prompt: "Delegate this task with the swingle-delegate skill."

policy:
  allow_implicit_invocation: false
